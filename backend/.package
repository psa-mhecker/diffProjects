cd /home/users/$user/$project
DATEDEB=`date '+%Y%m%d'`
if ! [ -d /home/users/$user/$project/_package ]
then
 mkdir -p /home/users/$user/$project/_package
fi

git diff --name-only $sha1 $sha2 | xargs zip /home/users/$user/$project/_package/$DATEDEB.zip